{
  "installCommand": "npm install --legacy-peer-deps",
  "buildCommand": "npx prisma generate && (npx prisma migrate deploy || echo 'migrate deploy skipped - table may need manual push') && next build"
}
